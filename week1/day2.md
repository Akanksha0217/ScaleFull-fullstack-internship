# 📅 Day 2 - June 17, 2025

## ✅ Topics Learned

- Revision of Day 1 HTML tags
- Using the `<img>` tag to add images:
  - Attributes: `src`, `alt`, `width`, `height`
- Adding hyperlinks using the `<a>` tag:
  - Attributes: `href`, `target`, `title`
- Understanding the `title` attribute (for tooltips)
- Using `width` and `height` to control image dimensions
- Difference between **Semantic** and **Non-Semantic** HTML tags

## 🛠️ Work Done

- Practiced embedding multiple images with different sizes
- Added hyperlinks with title attributes
- Compared semantic tags (`<header>`, `<main>`, `<footer>`) vs non-semantic tags (`<div>`, `<span>`)
- Created a sample layout using both semantic and non-semantic tags

## 💡 Notes

- The `title` attribute adds a tooltip when hovering over an element.
- **Semantic tags** clearly describe their meaning (e.g., `<article>`, `<nav>`)
- **Non-semantic tags** are generic containers (e.g., `<div>`, `<span>`)
- Use semantic tags for better **SEO**, **accessibility**, and **maintainability**

## 🔗 Resources

- [HTML Semantic Elements - MDN](https://developer.mozilla.org/en-US/docs/Glossary/Semantics)
- [HTML `<img>` Tag - W3Schools](https://www.w3schools.com/tags/tag_img.asp)
- [HTML `<a>` Tag - W3Schools](https://www.w3schools.com/tags/tag_a.asp)
